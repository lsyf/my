<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loushuiyifan.report.dao.RptRepfieldDefChannelDAO">


    <select id="list" resultType="com.loushuiyifan.report.bean.RptRepfieldDefChannel">
      select *
      from rpt_repfield_def_channel
      where  rpt_no = #{rptNo}
      order by index_id
    </select>

    <select id="listMap" resultType="map">
        select index_Code as "id",
               parent_index_id as "pid",
               index_Name as "name"
        from rpt_repfield_def_channel
        where  rpt_no = #{rptNo}
        order by index_id
    </select>

</mapper>